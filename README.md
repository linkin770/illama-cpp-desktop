# illama-cpp-desktop

一个功能完整的 Windows 本地 AI 部署桌面端控制面板，支持白色主题和深色模式切换。

项目提供桌面端控制面板，将启动服务、配置模型、查看日志、直接聊天和接入 OpenAI 兼容客户端等功能整合在一个窗口中。

---

## ✨ 功能特性

### 🚀 核心功能
- **OpenAI 兼容接口**: 标准 `/v1/chat/completions` 接口，可接入各类客户端（如 OpenClaw、Claude Code 等）
- **内置聊天页面**: 支持流式回复、历史对话搜索和管理
- **系统托盘后台运行**: 窗口最小化后服务继续运行
- **完整 llama.cpp 集成**: 支持 llama-server 服务管理和配置

### 🎨 用户体验
- **白色 / 深色模式一键切换**: 支持亮色和暗色主题
- **代码高亮与预览**: 支持多种编程语言的代码块展示
- **思考过程显示**: 可选显示模型推理过程
- **响应式设计**: 适配不同屏幕尺寸
- **发送按钮状态联动**: 输入内容/附件时变绿，生成时变红中止

### ⚙️ 高级配置
- **完整参数配置**: ctx_size、GPU 层数、采样参数等
- **实时终端日志**: 查看 llama.cpp 服务输出
- **模型信息查看**: 查看当前模型细节和运行参数
- **配置文件管理**: 支持 TOML 配置文件的导入导出

### 📁 文件支持
- **文本文件**: 支持多种文本格式解析
- **PDF 文件**: 自动提取文本内容（最大支持 100MB，根据 ctx_size 动态截断）
- **图片文件**: 支持视觉模型输入

---

## 📋 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 (64位) |
| Node.js | >= 18.0.0 |
| 内存 | 建议 >= 16GB (视模型大小而定) |
| GPU | 可选，支持 CUDA |

---

## 🚀 快速开始

### 前置准备

**下载 llama.cpp**

由于 llama.cpp 体积较大，本仓库不包含编译产物。请自行下载：

1. 访问 [llama.cpp 官方发布页面](https://github.com/ggml-org/llama.cpp/releases)
2. 下载 Windows 版本的发布包（如 `llama.cpp-win-cuda.zip`）
3. 解压后将所有文件复制到项目的 `llama/` 文件夹中

确保 `llama/` 文件夹包含以下关键文件：
- `llama-server.exe` - 主服务程序
- `llama.dll` - 核心推理库
- `ggml*.dll` - ggml 推理库
- `cublas*.dll` / `cudart*.dll` - CUDA 支持库（GPU版本）

### 方式一：直接使用（推荐）

1. 下载预编译的发布包
2. 解压后运行 `Llama.cpp Desktop.exe`
3. 在设置中选择 GGUF 模型文件
4. 点击"启动服务"按钮
5. 使用内置聊天，或把 `http://127.0.0.1:8080/v1` 接入 OpenAI 兼容客户端

### 方式二：源码运行

```powershell
# 克隆项目
git clone https://github.com/linkin770/illama-cpp-desktop.git
cd illama-cpp-desktop

# 下载 llama.cpp 并解压到 llama/ 文件夹
# 参考上方"前置准备"步骤

# 安装依赖
npm install

# 启动开发模式
npm start
```

---

## 📖 使用说明

### 配置模型

1. 点击侧边栏的"设置"按钮
2. 在"模型"选项卡中选择 GGUF 模型文件
3. （可选）配置 mmproj 投影文件（用于多模态模型）
4. 调整上下文长度、GPU 层数等参数
5. 点击"保存配置"

### 启动服务

1. 确保已配置好模型路径
2. 点击底部"启动服务"按钮
3. 等待服务启动（约几秒到几十秒，取决于模型大小）
4. 服务状态变为"运行中"后即可使用

### 接入外部客户端

将以下 URL 配置到支持 OpenAI API 的客户端中：
- **Base URL**: `http://127.0.0.1:8080/v1`
- **API Key**: 任意字符串（本地服务无需验证）

---

## 🔧 开发指南

### 项目结构

```text
├── desktop/          # Electron 主进程
│   ├── main.mjs      # 主进程逻辑（IPC、进程管理、配置处理）
│   └── preload.cjs   # 预加载脚本（安全的渲染进程桥接）
├── renderer/         # 渲染进程（前端界面）
│   ├── app.js        # 前端主逻辑（聊天、设置、UI 渲染）
│   ├── index.html    # HTML 入口
│   └── styles/       # CSS 样式文件（按模块拆分）
│       ├── variables.css      # CSS 变量定义
│       ├── base.css           # 基础样式
│       ├── layout.css         # 布局
│       ├── components.css     # 通用组件
│       ├── sidebar.css        # 侧边栏
│       ├── chat.css           # 聊天区域
│       ├── composer.css       # 输入框
│       ├── forms.css          # 表单
│       ├── service.css        # 服务栏
│       ├── terminal.css       # 终端
│       ├── attachments.css    # 附件
│       ├── settings.css       # 设置
│       ├── settings-extra.css # 设置额外
│       ├── extra.css          # 额外样式
│       └── dark-mode.css      # 暗色模式
├── llama/            # llama.cpp 编译产物
│   ├── llama-server.exe    # 主服务程序
│   ├── ggml*.dll           # ggml 推理库
│   └── cuda*.dll           # CUDA 支持库（可选）
├── assets/           # 图标和资源文件
└── scripts/          # 辅助脚本
```

### 核心技术栈

| 组件 | 版本 | 类型 | 说明 |
|------|------|------|------|
| Electron | 41.1.1 | devDependencies | 跨平台桌面应用框架，提供 Node.js 和 Chromium 运行环境 |
| electron-builder | 26.8.1 | devDependencies | 桌面应用打包工具，支持生成便携版和安装包 |
| fs-extra | ^11.2.0 | dependencies | 增强版文件系统操作库，支持异步文件读写、目录复制等 |
| pdf-parse | ^1.1.1 | dependencies | PDF 文本提取库，用于解析上传的 PDF 文件内容 |
| pdfjs-dist | ^4.4.168 | dependencies | Mozilla PDF.js 库，提供 PDF 解析能力 |

### 依赖说明

#### 开发依赖 (devDependencies)

**Electron**
- **作用**: 构建跨平台桌面应用的核心框架
- **版本**: 41.1.1 (稳定版)
- **功能**:
  - 提供主进程和渲染进程架构
  - 支持 Native API 调用（如系统托盘、对话框）
  - 集成 Chromium 浏览器引擎

**electron-builder**
- **作用**: 将 Electron 应用打包成可执行文件
- **版本**: 26.8.1
- **功能**:
  - 支持 Windows/macOS/Linux 多平台打包
  - 生成便携版（Portable）和安装程序
  - 支持 ASAR 打包优化

#### 运行依赖 (dependencies)

**fs-extra**
- **作用**: 增强 Node.js 文件系统操作
- **版本**: ^11.2.0
- **功能**:
  - 递归创建目录
  - 文件复制、移动、删除
  - Promise 化的异步操作
  - 在项目中用于读取配置文件和模型文件信息

**pdf-parse**
- **作用**: 从 PDF 文件中提取文本内容
- **版本**: ^1.1.1
- **功能**:
  - 解析 PDF 文档结构
  - 提取纯文本内容
  - 支持大文件解析
  - 在项目中用于处理用户上传的 PDF 附件

**pdfjs-dist**
- **作用**: Mozilla 官方 PDF 解析库
- **版本**: ^4.4.168
- **功能**:
  - 高性能 PDF 渲染
  - 文本提取和元数据读取
  - 与 pdf-parse 配合提供完整的 PDF 处理能力

### 开发命令

```powershell
# 启动开发模式
npm start

# 打包便携版
npm run dist

# 清理构建产物
rm -r dist
```

---

## ⚙️ 配置说明

### 配置参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `host` | 服务绑定地址 | 0.0.0.0 |
| `port` | 服务端口 | 8080 |
| `ctx_size` | 上下文窗口大小 | 32768 |
| `n_predict` | 最大输出 tokens | -1（无限制） |
| `n_gpu_layers` | GPU 加速层数 | 99 |
| `temp` | 温度参数 | 0.8 |
| `top_p` | Top-P 采样 | 0.95 |
| `top_k` | Top-K 采样 | 20 |

### 启动模式

- **Direct 模式**: 直接启动 llama-server.exe（推荐）
- **Launcher 模式**: 通过启动器启动（兼容旧版配置）

---

## ❓ 常见问题

### Q: 服务启动失败？

A: 请检查：
1. 模型文件路径是否正确
2. llama-server.exe 是否存在于指定目录
3. 日志窗口中的错误信息

### Q: 内存不足？

A: 尝试：
1. 减小 `ctx_size` 参数
2. 使用更小的模型（如 7B 模型）
3. 增加 `n_gpu_layers` 让更多层运行在 GPU 上

### Q: 如何接入其他客户端？

A: 将客户端的 API 地址设置为 `http://127.0.0.1:8080/v1`，API Key 可填任意值。

---

## 📝 更新日志

### v0.0.20260513 (2026-05-13)

#### 🎨 界面美化
- 采用温暖米色配色方案，打造现代化界面设计
- 聊天气泡采用渐变背景，增强视觉层次感
- 按钮组件重新设计，添加渐变效果和悬停动画
- 侧边栏历史记录采用卡片式设计
- 空状态页面重新设计，包含品牌 Logo 和友好欢迎语

#### 💬 聊天界面优化
- **用户消息右对齐**: 用户消息显示在右侧，模型回复在左侧，符合主流聊天软件习惯
- **附件右对齐**: 用户消息中的附件、操作按钮（复制/编辑/删除）统一右对齐
- **附件尺寸缩小**: 聊天中的图片附件改为小尺寸长方形，视觉更紧凑美观
- **滚动到底部按钮**: 重新定位至聊天区域底部，采用动态 JS 计算位置，适配不同窗口大小

#### 🎨 设计系统统一
- **圆角体系统一**: 全局统一使用 CSS 变量管理圆角（`--radius-sm`/`--radius-md`/`--radius-lg`/`--radius-xl`），所有组件级圆角统一为 `var(--radius-lg)` (14px)
- 涉及组件：聊天气泡、头像、附件卡片、侧边栏按钮、设置面板、输入框、代码块、历史记录、菜单、对话框等

#### 🖱️ 交互优化
- 修复滚动条自动滚动问题：流式输出时自动滚动，但用户手动拖动后停止跟随
- 修复切换历史对话时滚动条位置问题，默认滚动到底部
- 修复保存/启动服务时滚动条跳回顶部的问题
- 添加滚动条拖动检测，避免强制滚动与用户操作冲突

#### 🚀 代码重构
- **app.js 模块化拆分**: 将主入口文件按功能拆分为多个模块（constants.js、state.js、utils.js、icons.js、messages.js、chat.js、settings.js、components.js、events.js、render.js），提升代码可维护性和可读性

#### 🐛 Bug 修复
- 修复搜索历史对话时输入框失去焦点的问题
- 修复中文输入法无法连续输入的问题（IME 组合过程中不再重建搜索框 DOM）
- 修复搜索结果点击后触发 more 菜单而不是打开对话的问题
- 修复发送消息后界面不刷新的问题
- 修复图片上传后不立即显示的问题

#### 🎨 界面优化
- 重构渲染逻辑，采用增量更新策略，减少不必要的 DOM 操作

### v0.0.20260510 (2026-05-10)

#### 🚀 新功能
- **Word/Excel 文件支持**: 新增 `.docx`、`.xlsx`、`.xlsm`、`.xls` 文件解析，自动提取文本内容发给模型
- **附件菜单图标颜色**: 图片、音频、文本、PDF、Word、Excel、系统消息各配独立颜色

#### 🐛 Bug 修复
- 修复 Word/Excel 因 ESM 动态导入 CJS 模块默认导出未解包导致解析静默失败的问题
- 修复发送后附件不消失的问题（增量渲染优先级错误 + DOM 查找范围错误）
- 修复 fileBlocks 暴露本地文件路径导致模型误读
- 修复 mmproj 检查未重置 chatBusy 导致界面死锁
- 修复解析失败的文本类附件完全不可见的问题
- 修复历史菜单点击空白处不关闭的问题
- 删除暂不支持的视频文件菜单项

### v0.0.20260509 (2026-05-09)

#### 🚀 新功能
- **发送按钮状态联动**: 输入内容/附件时按钮变绿，生成中自动变红中止按钮，点击可停止流式输出
- **PDF 大文件支持**: 文件大小限制从 20MB 提升至 100MB，超过 50KB 自动提示分段提问
- **智能内容截断**: 根据 `ctx_size` 动态计算可读取的附件文本长度，避免超出上下文限制
- **新聊天自动入列**: 创建新聊天后立即出现在历史对话列表中，发送消息后自动更新标题

#### 🎨 界面优化
- **CSS 模块化拆分**: 将单文件 `styles.css`（3496 行）拆分为 15 个按功能模块划分的样式文件，提升可维护性
- **空聊天滚动支持**: 空聊天页面支持垂直滚动

#### 🐛 Bug 修复
- 修复发送后文本框和附件未清空的问题
- 修复上传文件后附件未立即显示的问题
- 修复输入文字后发送按钮不变色的问题
- 修复上传文件后菜单不自动关闭的问题
- 修复暗色模式下状态点颜色错误的问题
- 修复多次点击导致重复发送的问题

---

## 📄 许可证

MIT License

---

## 🤝 致谢

本项目基于以下优秀的开源项目：

- [llama.cpp](https://github.com/ggml-org/llama.cpp) — 高效的本地 LLM 推理引擎
- [illama-cpp-desktop](https://github.com/Qiao-920/llama-cpp-desktop) — llama.cpp Windows 桌面端控制面板

感谢原作者的出色工作！

---

**Enjoy AI on your desktop! 🦙**